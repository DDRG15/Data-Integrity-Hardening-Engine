-r requirements.txt
pytest>=8.0.0
pytest-mock>=3.12.0
pytest-cov>=5.0.0
# API service test deps: fastapi for the app under test, httpx for TestClient.
# These live here (not only in the [api] extra) so CI can collect and run
# tests/test_api.py -- a test file importing an absent dep kills collection
# for the whole suite (see V4.1.1 CI incident in docs/PROGRESS_LOG.md).
fastapi>=0.110
httpx>=0.27
